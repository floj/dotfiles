#!/usr/bin/env bash
set -euo pipefail
scriptName=$(basename "${BASH_SOURCE[0]}")

PATH=":$PATH:"
PATH="${PATH//:$HOME\/bin:/:}"
PATH="${PATH#:}"
PATH="${PATH%:}"
unset AWS_PROFILE
exec "$scriptName" "$@"
