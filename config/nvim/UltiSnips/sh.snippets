snippet fbash "bash options" b
#!/usr/bin/env bash
set -euo pipefail
script_dir="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"
script_name="$( basename "${BASH_SOURCE[0]}" )"
`!p snip.rv ='\n' `
endsnippet

# vim:ft=snippets:
